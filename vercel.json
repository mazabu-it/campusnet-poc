{
	"installCommand": "npm install && npm rebuild sharp",
	"buildCommand": "npm run payload migrate && npm run build",
	"crons": [
		{
			"path": "/api/payload-jobs/run",
			"schedule": "0 0 * * *"
		}
	]
}
